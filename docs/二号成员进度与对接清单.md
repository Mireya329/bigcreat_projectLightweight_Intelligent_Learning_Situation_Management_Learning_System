# 二号成员（Mireya）进度盘点与队友对接清单

> 依据：`大创统筹(1).docx` 第十节岗位分工 + 三十天分阶段开发任务
> 制表：2026-09-16（项目第 16 天，处于第三阶段 15–21 天）
> 职责原文：Python 后端 + OCR 底层 + AI 接口联动

---

## 一、我的职责清单（摘自统筹文档，逐条对照）

| 职责原文 | 状态 | 落地产物 |
|---|---|---|
| 全系统后端环境搭建 | ✅ 完成 | venv + PaddleOCR + Ollama + Gradio（部署日志） |
| 开源源码二次开发 | 🟡 进行中 | 数据库 + 切分 + 入库 + API（**前端改造未做**） |
| SQLite 数据库搭建与维护 | ✅ 完成 | 11 张表，`data/learning.db` |
| PaddleOCR 离线识别参数调优 | ✅ 完成（任务 A） | 整卷 2 小时 → **73 秒**，13/13 切片全成功 |
| **错题数据分类逻辑开发** | ⬜ **未做** | 第二阶段遗留，`error_type` 字段已预留但未填 |
| AI 接口对接 | ✅ 完成（任务 B） | `ai_interface.py`，5 个场景 + 2 个实验场景 |
| 前后端数据互通 | ✅ 完成 | FastAPI 9 个接口 + `/docs` 文档 |
| **Anki 数据同步脚本** | ⬜ **未做** | 第三阶段任务 |
| **学情统计算法开发** | 🟡 部分 | `study_stats.py` 有统计，**未成"算法/诊断"** |

---

## 二、按三十天计划的进度

### 第一阶段（第 1–7 天）✅ 全部完成

- 环境搭建、克隆 5 个开源项目、PaddleOCR 部署、预留 AI 接口端口

### 第二阶段（第 8–14 天）三项任务

| 任务 | 状态 | 说明 |
|---|---|---|
| A. 深度调优 PaddleOCR 参数 | ✅ | 4.26s/片，整卷 73 秒，13/13 |
| B. 打通后端与 AI 模型数据通道 | ✅ | 0.5b 端到端跑通，四段结构达标 |
| C. 二次开发单词、错题系统后端 | 🟡 | 主体完成，**"错题数据分类逻辑"这一项未做** |

### 第三阶段（第 15–21 天，当前）

| 任务 | 状态 |
|---|---|
| Anki 数据双向同步脚本 | ⬜ 未开始 |
| 学情统计算法 | 🟡 有统计，缺算法 |
| 协助组长 AI 接口联调 | 🟡 接口已就绪，等 7b |

---

## 三、我已经做完的东西（可交付）

**数据库**：11 张表（用户/学科/知识点/错题/标签/复习计划/词库/单词/练习记录/AI日志…）
**错题链路**：OCR → 切分（召回 75%、精确率 100%）→ 质量门禁 → 入库 → AI 解析
**单词链路**：2607 个 CET4 词 + 200 条练习记录 + 跨线统计
**接口层**：9 个 HTTP 接口，带交互式文档，冒烟测试全绿
**实测数据**：OCR 73 秒/卷、AI 解析 6.9 秒/题、切分精确率 100%

---

## 四、我自己的缺口（按优先级）

| 优先级 | 缺口 | 归属阶段 | 预估 |
|---|---|---|---|
| P0 | **错题数据分类逻辑**（`error_type`：概念不清/计算失误/审题偏差/方法缺失） | 第二阶段遗留 | 2h |
| P0 | **等 7b 模型**（组长提供，否则 AI 功能不可演示） | 依赖组长 | — |
| P1 | **Anki 数据双向同步脚本** | 第三阶段 | 4h |
| P1 | **学情统计算法**（不只是统计，要有薄弱度打分/趋势） | 第三阶段 | 3h |
| P2 | **考研院校数据表**（库里还没有，4 号负责建模） | 依赖 4 号 | 1h 建表 |
| P2 | **题库表**（四大题库：校内/期末/证书/考研真题） | 依赖 4 号 | 1h 建表 |

---

## 五、需要对接的队友

### 👤 1 号组长（AI 总负责）

**要什么**：
1. **7B 模型什么时候能部署** —— 现在只有 0.5b，实测它**只能验证格式**，数学解析和学情诊断都是幻觉（诊断会编出"英语阅读理解"这种我数据里根本没有的弱项）。没有 7b，AI 模块无法演示
2. **AI 输入输出协议确认** —— 我已预留 `ai_interface.py`（5 个场景：错题解析/问答/诊断/择校/作文），请确认字段是否满足他那边的 Prompt 设计
3. **错题分类的输出格式** —— 我准备做 `error_type` 自动分类，他那边 Prompt 里已有四类（概念不清/计算失误/审题偏差/方法缺失），需要约定 AI 返回什么样我好解析入库

**可直接发**：
> 组长，我这边后端接口和数据库都好了。两个事：①7B 模型大概什么时候能部署？现在 0.5b 我实测只能验证格式，解析和诊断都会幻觉，没 7b 的话 AI 这块我没法往下做联调测试。②我的 ai_interface.py 里预留了 5 个 AI 场景（错题解析/问答/诊断/择校/作文），你看下字段够不够用；另外我准备做错题自动分类，你 Prompt 里那四类（概念不清/计算失误/审题偏差/方法缺失）返回的格式能不能定死，我好写解析。

### 👤 3 号（前端可视化 + ECharts）

**要什么**：
1. **给她接口** —— `GET /stats`（跨线学情统计）、`GET /words/wrong-top`（易错词）、`GET /errors`（错题列表）、`GET /review/due`（待复习）。文档在 `docs/后端接口对接文档.md`
2. **向她要图表字段需求** —— 她要画"雷达图/饼图/趋势曲线"，现在我的 `/stats` 返回的是我猜的字段，**可能需要按她的图表改**
3. 提醒她：`trans`（单词释义）是 JSON 字符串要 parse；`ocr_quality < 70` 的题不要展示 AI 解析

**可直接发**：
> 3 号，后端接口好了，文档在 `docs/后端接口对接文档.md`，服务起来后打开 http://127.0.0.1:8000/docs 能直接试。学情统计 `/stats`、易错词 `/words/wrong-top`、错题列表 `/errors` 都能用。你那边 ECharts 要画雷达图和趋势曲线，看下 `/stats` 现在的字段够不够，缺什么告诉我，我加。

### 👤 4 号（题库 + 考研大数据 + 测试 + 文档）

**要什么**：
1. **题库目录规范** —— 她负责四大题库（校内专业课题库/期末真题/证书/考研真题），我需要她的**目录结构和文件命名规则**，才能写批量导入脚本
2. **考研数据表结构** —— 她负责考研数据采集与建模，但表要建在我的库里（她说字段，我建表+索引）
3. **给她测试数据** —— 我这套 OCR + 切分 + 入库流程需要她用真实试卷跑批量测试（她第二阶段任务里有"批量测试 OCR 识别效果"）
4. **给她我的日志** —— `docs/第二阶段错误日志.md`（已 24 条）是她写结题报告"调优过程"的原材料

**可直接发**：
> 4 号，两件事对接下：①你的四大题库目录结构和命名规则定下来了吗？定了我写批量导入脚本导进数据库。②考研院校数据你要存哪些字段？给我字段清单我在 SQLite 里建表。另外我这边 OCR + 入库跑通了（整卷 73 秒），你有真题试卷可以拿来批量测一下，我的错误日志在 `docs/第二阶段错误日志.md`，结题材料要用可以直接取。

---

## 六、一句话总结

**我该做的**：错题分类逻辑（补第二阶段欠账）→ Anki 同步脚本 → 学情统计算法
**我在等的**：1 号给 7b 模型和 AI 协议、3 号给图表字段需求、4 号给题库/考研表结构
**我可以先给别人的**：接口文档（已就绪）、错误日志（已就绪）
