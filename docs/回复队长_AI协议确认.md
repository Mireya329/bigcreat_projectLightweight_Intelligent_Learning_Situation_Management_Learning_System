
协议收到，我这边已经按你的要求改完了，说四件事。

**一、协议全部落地了**

- 场景名改成你说的五个：`error_analysis` / `qa` / `weak_diagnosis` / `school_recommend` / `essay_review`（我原来的旧名做了别名映射，老代码不用动）
- 请求带 `subject` 和 `user_context`
- 返回改成 `{code, scene, data, raw, model, elapsed_ms}`
- `format=json` 已开；收到后校验字段，不合格自动重试 1 次，还是不行就降级：保留 raw、置 code=1、**原文绝不入库**（只写进 ai_logs 供排查）
- error_type 只用你那四个 code，`error_confidence < 0.5` 打"待人工复核"，error_type 不入库

我写了套自测把上面每条都跑了一遍（模拟模型返回散文、缺字段、非法 code、服务挂掉等情况），8 组全过。你如果要看可以跑：
`venv\Scripts\python.exe src\backend\ai_protocol_test.py`

**二、有个实测结果得同步给你**

我拿库里现有的 7 道已解析错题按新规则重判：**只有 1 道能自动入库，6 道被判待人工复核**。

原因是现在 0.5b 在自由文本模式下，习惯把四类一起列出来当模板背（"错误原因：概念不清、计算失误、审题偏差、方法缺失"）——这不是分类，是背书。所以 5 道置信度直接判 0。

也就是说：**我侧的校验和降级都好了，但当前 0.5b 的质量撑不起自动入库**。你那边 7b 跑通后强制 JSON 四选一，应该会好很多，到时候我们再全量重跑一次对比。

**三、择校那个我先试了**

`school_recommend` 场景按协议写好了（冲/稳/保三档 JSON），但**没有院校库数据**，现在跑出来只能是空的。这块得等 4 号把考研院校表字段给出来才能出真实推荐，我先按占位放着。

**四、五个需要你定的点**

1. 7b 到位后，是我直连本机 Ollama（我改个模型名就行），还是你另起一个 HTTP 服务给我地址？两种我都留了开关。
2. `judge_fragment` 是我自己加的场景（OCR 切分漏题兜底），不在你那五个里面，能不能沿用同一套返回结构？
3. 择校等 4 号院校表，你知道他那边大概什么时候能给吗？
4. `user_context` 你希望我传哪些学情字段？我现在默认传的是学情统计的 summary。
5. 7b 到位后重跑，我倾向 `--clear` 全量重跑保证口径一致，你同意吗？

---
