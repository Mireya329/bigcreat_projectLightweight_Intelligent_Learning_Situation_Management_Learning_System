# 二号成员（Mireya）对接确认表

> 请各位对照下表确认，**在「对方确认」栏回复"确认"或写明问题**。
> 更新时间：2026-09-16

| # | 对接对象 | 事项 | 流向 | 具体内容 / 要求 | 期望时间 | 对方确认 |
|---|---|---|---|---|---|---|
| 1 | 1号组长 | 7B 模型部署时间 | 组长 → 我 | 现只有 0.5b，实测仅能验证格式，数学解析与学情诊断均为幻觉。**没有 7b，AI 模块无法联调与演示** | 越早越好 | |
| 2 | 1号组长 | AI 接口协议确认 | 双向 | 我已预留 5 个场景（错题解析 / 知识点问答 / 薄弱点诊断 / 择校推荐 / 作文批改），请确认 `ai_interface.py` 字段是否满足你的 Prompt 设计 | 本周 | |
| 3 | 1号组长 | 错题分类输出格式 | 组长 → 我 | 我要做错题自动分类（`error_type`），请把四类（概念不清 / 计算失误 / 审题偏差 / 方法缺失）的**返回格式定死**，我好解析入库 | 本周 | |
| 4 | 3号（前端可视化） | 学情图表字段需求 | 3号 → 我 | `/stats` 现在返回的字段是我推的，你看下够不够画**雷达图 / 饼图 / 趋势曲线**，缺什么我加 | 本周 | |
| 5 | 3号（前端可视化） | 接口文档交付 | 我 → 3号 | 已就绪：`docs/后端接口对接文档.md`；服务启动后打开 `http://127.0.0.1:8000/docs` 可直接试 | 已交付 | |
| 6 | 3号（前端可视化） | 两个字段约定（避免隐性 bug） | 我 → 3号 | ①单词 `trans` 是 **JSON 字符串**，前端要 `JSON.parse()`；②`ocr_quality < 70` 的题**被门禁拦下，不要展示 AI 解析** | 已知会 | |
| 7 | 4号（题库/大数据） | 四大题库目录与命名规范 | 4号 → 我 | 给我目录结构 + 文件命名规则，我写批量导入脚本导进数据库 | 本周 | |
| 8 | 4号（题库/大数据） | 考研院校数据表字段 | 4号 → 我 | 给我字段清单，我在 SQLite 建表 + 索引（库里目前还没有考研表） | 本周 | |
| 9 | 4号（题库/大数据） | 真实试卷批量测试 | 我 → 4号 | OCR + 切分 + 入库已跑通（整卷 73 秒），可拿真题批量测，结果记进错误日志 | 下周 | |
| 10 | 4号（题库/大数据） | 错误日志交付 | 我 → 4号 | 已就绪：`docs/第二阶段错误日志.md`（24 条，含全部实测数据与踩坑），结题材料可直接取用 | 已交付 | |

## 补充：3 号《前端冗余模块删减清单》中点名 2 号确认的 4 项

> 来源：`副本前端冗余模块删减清单1(1).xlsx` → 工作表「待确认问题」
> 该清单制作于 2026-08-27，以下 4 项至今仍为"待确认"

| # | 3 号的问题 | 我的答复（待 3 号确认） | 状态 |
|---|---|---|---|
| 1 | 系统是否需要用户登录/账号体系？ | **不需要**。本地单机单人应用，电脑即账号。库里保留 `users` 表但只用一个默认本地用户（`username=test`），**前端不做登录页/注册页/个人中心** | 待 3 号回 |
| 4 | 五大科目后端字段标识是什么？前端筛选要匹配 | 见下表「科目字段标识方案」 | **需 3 号按此实现筛选** |
| 6 | Wrong-Notebook 的 login/register 页保留吗？ | **删除**。同第 1 项，不做账号系统 | 待 3 号回 |
| 8 | Wrong-Notebook 的 tags 标签管理保留吗？ | **保留**。后端已实现：`knowledge_tags`（知识点）+ `error_item_tags`（错题↔知识点多对多），9 道错题已打标签 | 待 3 号回 |

### 科目字段标识方案（我定，供前端筛选）

| code（英文标识，稳定） | name（中文显示） | 说明 |
|---|---|---|
| `POSTGRAD_MATH` | 考研数学 | 现有"数学"错题归入此科 |
| `POSTGRAD_ENGLISH` | 考研英语 | |
| `POSTGRAD_POLITICS` | 考研政治 | |
| `CET4` | 英语四级 | 词库 CET4_T 归属此科 |
| `CET6` | 英语六级 | |

- 建议在 `subjects` 表增加 `code` 字段（需一次 ALTER TABLE 迁移），前端筛选**用 code、显示用 name**
- 迁移后现有 9 道错题从"数学"改挂 `POSTGRAD_MATH`

## 说明

- **第 1 项是目前最大的阻塞**：7b 不到，AI 相关功能只能验证链路、不能验证效果
- **第 7、8 项我这边在等**：表结构确定后建表只需 1 小时，但字段必须由数据负责人定
- 我目前**可独立推进**的（不依赖任何人）：错题分类逻辑、Anki 同步脚本、学情统计算法
