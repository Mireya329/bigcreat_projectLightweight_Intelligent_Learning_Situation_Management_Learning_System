# 第二阶段 · 实操日志（2号成员）

> 记录规范：每条按"时间 / 操作 / 结果 / 分析"记录，报错原样粘贴。
> 用途：结题材料"调优过程"证据 + 复现依据。

---

## 条目 1：OCR 性能基线测量（2026-09-13 16:33）

**操作**：单片（slice_00.png，1280×300）同一进程连续 predict 两次，测冷/热耗时。

```python
ocr = PaddleOCR(use_textline_orientation=True, lang='ch', enable_mkldnn=False)
result = ocr.predict(r"D:\bigcreat_project\data\ocr_real_test\slices\slice_00.png")
```

**结果**：
- 第 1 次（冷，含模型加载）：**51.65 秒**
- 第 2 次（热，纯推理）：**18.82 秒**

**分析**：
1. 模型加载约占 33 秒（51.65 − 18.82），5 个模型共 177MB → **常驻单进程可省掉这部分**
2. 纯推理 18.82 秒/片 → 13 片 ≈ 245 秒 ≈ 4 分钟/整卷。对比第一阶段实测的 ~600 秒/片，**快了 30 倍以上**——说明第一阶段慢的主因是"多进程并发抢 CPU + 每个进程重复加载模型"，不是 paddle 本身慢
3. **重要反转**：第一阶段记录的"同进程第 2 次 predict 会状态污染失败"这次**没有复现**，同一进程连跑两次完全正常。说明污染问题很可能也是并发干扰引起的，单进程串行方案可行性大增
4. 距离目标（整卷 30 秒内）还差 8 倍，继续调：先试 `text_det_limit_side_len` 降检测分辨率，再试 mobile 轻量模型

---

## 条目 2：调参 `text_det_limit_side_len=960`（2026-09-13 16:38）

**操作**：在 STEP 1 基础上唯一变化——PaddleOCR 初始化增加 `text_det_limit_side_len=960, text_det_limit_type='max'`（检测输入超过 960px 就缩到 960）。

```python
ocr = PaddleOCR(use_textline_orientation=True, lang='ch', enable_mkldnn=False,
                text_det_limit_side_len=960, text_det_limit_type='max')
```

**结果**：
- 第 1 次（冷）：**6.46 秒**（本次模型加载异常快，应该是磁盘缓存已热）
- 第 2 次（热）：**6.32 秒**
- 对比基线：**18.82s → 6.32s，提速约 3 倍** ✅

**分析**：
1. 检测分辨率 1280→960，检测阶段计算量降约 44%，整体 3 倍提速说明检测阶段确实是耗时大头
2. 换算整卷：13 片 × 6.3s ≈ **82 秒**，已接近目标（30s/整卷），还差 2.7 倍
3. 识别质量检查（行数+前5行文本）结果待用户回报，**若小字漏检则需权衡或改走 mobile 模型路线**

---

## 条目 3：交互环境粘贴多行代码报错（2026-09-13 16:42）

**操作**：在 Python 交互环境里粘贴多行 for 循环做质量检查。

**报错**（3 个，全部是语法/缩进类）：
```
SyntaxError: invalid syntax            ← 粘贴进了未结束的 ... 续行块
IndentationError: unexpected indent    ← 带缩进的行单独粘贴
IndentationError: unexpected indent
IndentationError: unexpected indent
```

**原因**：
1. 第一次粘贴时上一条 for 语句还在等待缩进块（提示符是 `...` 而不是 `>>>`），新代码粘进去把块结构弄乱 → SyntaxError
2. 之后带前导空格的行（如 `    print(...)`）被逐行单独执行，交互模式不允许"裸"缩进 → IndentationError

**教训（交互模式粘贴铁律）**：
- 看提示符：`>>>` 才是干净状态，`...` 说明上一条还没结束（先按回车/退格清掉）
- 多行带缩进的块在交互模式极易粘贴出错 → 要么写成单行（`for t in x[:5]: print(t)`），要么存成 `.py` 文件跑
- 数据分析这类多行代码建议直接写脚本文件，别在交互环境拼

**解决**：改用无缩进的单行写法重跑质量检查（见条目 4）。

---

## 条目 4：960 参数质量检查结果（2026-09-13 16:44）

**结果**：识别行数 6 行——厦门理工学院 / 班级 / 姓名 / 学号 / 习题二偏导数与高阶偏导数（合并成一行）

**与基线（1280 检测，7 行）对比**：
| 基线 | 960 后 |
|---|---|
| 厦门理工学院 ✅ | ✅ |
| 班级 / 姓名 / 学号 ✅ | ✅ |
| 习题二 + 偏导数与高阶偏导数（两行） | ⚠️ 合并成一行 |
| 一、选择题 | ❌ **丢失** |

**分析**：
1. 速度 3 倍提升的代价：丢了 1 行内容（"一、选择题"），另有 2 行被合并（合并无伤大雅，文本还在）
2. "一、选择题"是正文行，丢了会影响错题切分 → 不能直接接受
3. 下一步：试中间值 1088，看能否在速度和质量之间找到平衡点（调参就是找这个平衡）

---

## 条目 5：分辨率定版 1088（2026-09-13 17:11）

**操作**：`text_det_limit_side_len=1088, text_det_limit_type='max'`，同样测冷热 + 质量检查。

**结果**：
- 第 1 次：8.17 秒 / 第 2 次（热）：**7.17 秒**
- 识别 6 行：厦门理工学院 / 班级 / 姓名 / 学号 / 习题二偏导数与高阶偏导数 / **一、选择题（回来了！）**

**结论**：
| 方案 | 热耗时/片 | 内容覆盖 |
|---|---|---|
| 基线 1280 | 18.82s | 7 行（全） |
| 960 | 6.32s | 丢 1 行 ❌ |
| **1088 定版** | **7.17s** | **内容全覆盖（仅 1 处合并，可接受）** ✅ |

**分析**：比 960 只慢 0.85 秒换回完整内容覆盖，值得。**分辨率调参到此定版：1088**。
整卷估算：13 片 × 7.2s ≈ **94 秒**。距离 30 秒目标还差 3 倍 → 下一步换 mobile 轻量模型（推理更快的另一大杠杆）。

---

## 条目 6：换 PP-OCRv5_mobile 轻量模型（2026-09-13 17:18）

**操作**：模型名指定 `PP-OCRv5_mobile_det` + `PP-OCRv5_mobile_rec`（保留 1088 分辨率限制）。首次初始化自动从 modelscope 下载模型（6 文件，3 秒完成，很顺利）。

**结果**：
- 第 1 次（冷）：3.54 秒
- 第 2 次（热）：**3.28 秒**
- 识别行数：6 行（与 1088+medium 持平）
- 前 5 行文本质量：待用户回报

**分析**：
1. 热耗时 7.17s → 3.28s，**再提速 2.2 倍**；整卷估算 13 × 3.3 ≈ **43 秒**，距离 30 秒目标一步之遥
2. 累计战绩：18.82s → 3.28s，**提速 5.7 倍**
3. 行数持平是好兆头，但 mobile 精度风险在小字和低置信区域，必须看文本内容才能定版

---

## 条目 8：mobile 模型质量揭晓——字错了（2026-09-13 17:25）

**结果**（1088 + mobile，热 3.28 秒，6 行）：

| 基线文本 | mobile 识别 | 判定 |
|---|---|---|
| 厦门理工学院 | 厦门理工学院 | ✅ |
| 班级 / 姓名 / 学号 | 同 | ✅ |
| 习题二 | 习题二 | ✅（反而把 medium 的合并行拆开了） |
| 偏导数与**高阶偏**导数 | 偏导数与**高粉编**导数 | ❌ 3 个字认错 |
| 一、选择题 | 、选择题 | ⚠️ 丢"一"字 |

**分析**：
1. mobile 证实了精度风险：单字错误（"阶偏"→"粉编"）对错题系统是致命的——喂给 AI 的题目文本错了，AI 解析必歪
2. 但速度代价太诱人（3.28s vs 7.17s）。关键洞察：**检测（det）才是耗时大头**（STEP 2 降 det 分辨率就提速 3 倍），识别（rec）只是对裁出来的小图逐行辨认
3. 由此引出混合方案：**mobile 检测 + medium 识别**——用 mobile 的速度做检测，用 medium 的精度做识别，两头兼得。medium_rec 模型本地已有，不用下载

---

## 条目 9：混合方案（mobile_det + medium_rec）实测（2026-09-13 17:28）

**结果**：热 **3.93 秒**（≤4.5 达标），6 行：
- 厦门理工学院 / 班级 / 姓名 / 学号 / 习题二 / 偏导数与**高阶偏**导数（✅ 关键字全对，medium 识别精度回来了）
- 但 **、选择题 仍丢"一"字** ⚠️

**分析**：
1. 字错问题解决（rec 换 medium 生效），但"一"还丢——识别模型已经是 medium 还丢，说明问题出在**检测端**：mobile_det 把"一"的框漏了或裁掉了（"一"就一横，检测框小、易被吞）
2. 印证"耗时大头在检测"：混合方案 3.93s ≈ 全 mobile 3.28s，识别换 medium 几乎不增加时间
3. 备选动作：把 det 分辨率限制从 1088 放回 1280（我们的切片本来就是 1280 宽，等于不对检测缩放），看 mobile_det 能否找回"一"，同时观察时间涨幅

---

## 条目 10：最终配置定版！（2026-09-13 17:30）

**最终配置**：`PP-OCRv5_mobile_det`（检测）+ `PP-OCRv6_medium_rec`（识别）+ `text_det_limit_side_len=1280` + `enable_mkldnn=False`

**结果**：热 **4.26 秒**，识别 **7 行**，与第一阶段基线**逐字全对**：
```
厦门理工学院 / 班级 / 姓名 / 学号 / 习题二 / 偏导数与高阶偏导数 / 一、选择题
```

**调优全程成绩单（单slice_00，18.82s → 4.26s，提速 4.4 倍，零精度损失）**：
| 配置 | 热耗时 | 质量 |
|---|---|---|
| 基线（medium+1280） | 18.82s | 7 行全对（标杆） |
| medium+960 | 6.32s | 丢 1 行 ❌ |
| medium+1088 | 7.17s | 全对但 1 处合并 |
| 全 mobile+1088 | 3.28s | ❌ 3 个字错 |
| mobile_det+medium_rec+1088 | 3.93s | ⚠️ 丢"一"字 |
| **mobile_det+medium_rec+1280 定版** | **4.26s** | **7 行逐字全对** ✅ |

**结论**：
1. 验证了假设：丢"一"是检测分辨率不够（1088→1280 后 mobile_det 找回了它）
2. 混合方案（轻检测+中识别）是速度与精度的最优平衡，成本几乎为零
3. 整卷预估：13 片 × 4.26s ≈ **55 秒**（单进程串行，含一次模型加载）→ 待整卷实测验证（STEP 7）

---

## 条目 11：STEP 7 整卷实测——任务 A 结项！（2026-09-13 21:21）

**操作**：新脚本 `src\backend\ocr_fast_run.py`（单进程串行跑 13 片，定版配置），用户手动建文件并运行成功。

**结果**：
```
13/13 片全部成功（第一阶段只有 11/13！）
整卷总耗时: 72.59 秒（单进程串行，含一次模型加载）
总识别行数: 116
单片耗时: 2.90 ~ 11.14 秒
产物: data\ocr_fast_test\（13 个 slice_XX.json + all_text.txt）
```

**新旧对比（结题报告可直接引用）**：
| 指标 | 第一阶段 | 第二阶段调优后 | 提升 |
|---|---|---|---|
| 整卷总耗时 | ~2 小时（600s/片×11片） | **72.59 秒** | **~100 倍** |
| 切片成功率 | 11/13（并发干扰挂 2 片） | **13/13** | 状态污染问题消失 |
| 进程模型 | 13 进程并发 | 单进程串行 | 架构简化 |
| 识别质量 | 182 行（含大量低置信碎片） | 116 行 | 关键内容等价* |

*质量说明：题号覆盖对比（旧 1~5 / 新 1~5）一致；页眉 7 行逐字全对；行数减少主要因为 mobile_det 检出的低置信碎片行更少（旧结果里 0.386、0.483 这类碎行在新结果中消失），公式主体两边质量相当。待办：任务 C 做错题切分时再全面评估内容召回。

**任务 A 结项结论**：PaddleOCR 调优完成——定版配置 `PP-OCRv5_mobile_det + PP-OCRv6_medium_rec + det_limit 1280 + enable_mkldnn=False`，单进程串行流水线，整卷 2 小时 → 73 秒。

---




## 条目 7：续行块里粘贴单行 for → SyntaxError（2026-09-13 17:21）

**报错**：
```
File "<stdin>", line 2
    for t in r3['rec_texts']: print("  ", t)
    ^^^
SyntaxError: invalid syntax
```

**原因**：条目 3 的老问题复发——上一条 for 循环还在 `...` 续行状态，新的一行 for 被粘进去变成了嵌套块的第二行，语法非法。干净粘贴后已恢复 `>>>`。

**教训强化**：粘贴前**必须先确认提示符是 `>>>`**；在 `...` 状态下要么按回车执行完、要么 Ctrl+C 取消，再粘新代码。



## 条目 12：任务 B · STEP 8 拉取测试模型（2026-09-14 21:53）

**操作**：`D:/Ollama/ollama.exe pull qwen2.5:0.5b`（用 0.5B 小模型先打通管道，正式的 qwen2.5:7b 由组长第二阶段部署时再拉，省 4.3GB 流量）。

**结果**：
```
397 MB/397 MB @ 3.4 MB/s → verifying sha256 digest → success
ollama list 确认：qwen2.5:0.5b  a8b0c5157701  397 MB
```

**分析**：Ollama 官方源国内直连可用（比当时下 Ollama 安装包顺利多了），模型落在 D:/Ollama/models。通道就绪，下一步端到端联调（OCR 文本 → AI 解析）。

---

## 条目 13：任务 B · STEP 9 端到端联调成功——任务 B 结项！（2026-09-14 22:10）

**操作**：①改 ai_interface.py 配置（MOCK_MODE=False、MODEL_NAME=qwen2.5:0.5b）；②新建 src\backend\ocr_ai_pipeline.py；③运行：all_text.txt → ocr_result_to_question() → explain_wrong_question() 真实推理。

**结果**：
```
Ollama 在线: True
题目文本共 990 字符（OCR 全文喂入）
AI 耗时 96.3 秒（含首次模型加载进显存）
```
AI 输出**严格遵循 Prompt 模板的四段结构**：1.正确解题思路 2.错误原因归类（概念不清/计算失误/审题偏差/方法缺失，四类齐全）3.关联知识点（偏导数与高阶偏导数的定义）4.变式题。

**质量评估（0.5b 测试模型）**：
- ✅ 格式：四段结构完整，说明第一阶段冻结的 Prompt 模板有效
- ✅ 主题：正确抓住了"偏导数与高阶偏导数"
- ❌ 数学推理：内容空泛，没有真正解题（"玩具模型"预期之内）

**给组长的实证结论**：0.5b 能撑起接口协议和输出格式，但数学解析必须上 7b 及以上——这组对比数据可直接用于第二阶段模型选型汇报。

**任务 B 结项结论**：数据通道全链路打通——OCR 文本 → ocr_result_to_question() → call_ai() → Ollama(11434) → 真实推理输出。切换正式模型只需改 MODEL_NAME 一行。

## 条目 14：任务 C · STEP 1~6 统一数据库完工（2026-09-15 16:47~17:50）

**操作**：按 docs/任务C第一步_统一数据库行动计划.md 执行 STEP 1~6：
存 schema.sql（11 张表）→ init_db.py 建库 → db.py 统一连接层 → seed.py 种子数据
→ test_crud.py CRUD 冒烟 → import_dict.py 导入 CET4 词库

**结果**：
- data/learning.db 建成：11 张业务表，单词线（dicts/words/word_records/chapter_records）
  与错题线（error_items/review_schedules/error_item_tags）共用一套 user_id
- 种子数据：1 用户 / 2 学科 / 1 知识点 / 1 条错题（OCR 全文 995 字符入库）+ 标签关联
- 外键级联删除实测有效（error_item_tags 2→1，review_schedules 1→0）
- CET4 导入：dicts 1 条 / words 2607 条，重复运行无脏数据，前 3 词抽查正确

**分析**：
1. 单词/错题统一库的收益：学情统计可跨线 JOIN，不用维护两套库
2. PRAGMA foreign_keys 每个连接都要单独开，已封装进 db.py 的 get_conn()，
   全项目只准从这里连库（防止有人漏写导致外键静默失效）
3. 踩坑记录见条目 15


---

## 条目 15：建库过程踩坑合集（2026-09-15）

| # | 报错/现象 | 原因 | 解决 |
|---|---|---|---|
| 1 | `无法加载模块"venv"` | 在子目录里执行，相对路径找不到 venv | 每次先 cd 项目根再跑 |
| 2 | `[Errno 2] No such file or directory` | 文件名打成 init.db.py（点号） | Errno 2 基本都是路径/文件名对不上 |
| 3 | `NameError: '__file_' is not defined` | dunder 前后各两个下划线，打成了一个 | 照报错里的 Did you mean 抄 |
| 4 | `TypeError: 'NoneType' object is not subscriptable` | 学科 INSERT 被误覆盖，subjects 空 → fetchone() 返回 None | fetchone() 为 None 要往上看插入代码，别只盯报错行 |
| 5 | ocr 长度 995 ≠ 文件 1533 | 字节 vs 字符：UTF-8 汉字 3 字节 | length() 数字符，文件大小数字节 |

---

## 条目 16：任务 C · STEP 8 错题切分实验——7/10 题（2026-09-15 18:10~18:26）

**操作**：新建 `src\backend\ocr_split.py`，用两条正则对 `all_text.txt`（116 行）切题：
- 章节：`^([一二三四五六七八九十]+)、(.+)$`
- 题号：`^(\d+)\s*[.．、](.+)$`（兼容 `1.`、`4.设`、`3、` 三种写法）

**结果**：
```
共切出 7 道题（选择题 1 / 填空题 2 / 计算题 4）
第1题 选项数 7 个，其余 0 个（符合预期：只有选择题有选项）
```

**分析（本次核心发现：OCR 的题号识别不可靠）**：

| 应切 | 实切 | 丢失原因 |
|---|---|---|
| 选择题 3 题 | 1 题 | 第 1 题题号被 OCR 认成 `Ox100001`（"1." 识别失败）；第 3 题题号行整行丢失，仅残留孤立选项组 |
| 填空题 2 题 | 2 题 | ✅ 完整 |
| 计算题 5 题 | 4 题 | 第 2 题题号行丢失，内容混在手写笔记区（行 68~84） |

**召回率 7/10 = 70%**。结论：**纯正则只能做粗切**，题号丢失是 OCR 的固有缺陷，不是代码 bug。

**下一步补救方案（按成本递增）**：
1. 人工标注 `golden_splits.json` 作为切分验收基准（没有标准答案的优化都是盲调）
2. 选项块二次切分：用 `(A)(B)(C)(D)` 反推选择题边界（可救回选择题第 1、3 题）
3. AI 兜底：孤儿行块整段喂 AI，判断"是上一题延续还是新题"

**踩坑**：正则错误分两类，第二类更危险：
- **语法错**：`\(` 敲成 `$` → 当场报 `re.error: nothing to repeat at position 2`
- **语义错**：漏写 `\)` → **不报错，静默返回 0 个匹配**。只有验收标准能兜住，所以必须先试正则、人工核对再进代码

---

## 条目 17：任务 C · STEP 13 AI 解析闭环跑通（2026-09-15 19:05~19:12）

**操作**：Ollama（qwen2.5:0.5b）启动后，新建 `src\backend\ocr_ai_batch.py`，
从 `error_items` 取 `analysis IS NULL` 的错题 → 调 `explain_wrong_question()`
→ 结果写回 `analysis` / `ai_model` / `ai_elapsed_ms`，并在 `ai_logs` 记一条。

**结果（9 道全跑完）**：

| 指标 | 数值 |
|---|---|
| 解析题数 | 9 / 9，剩余待解析 0 |
| 平均耗时 | 6.9 秒（最快 5.5s，最慢 12.4s 含首次加载） |
| 平均输出 | 674 字符 |
| 输出格式 | 四段结构（解题思路/错误原因归类/关联知识点/变式题）**9 题全部完整** |

**分析**：

1. **格式层面 100% 达标**——再次验证第一阶段冻结的 Prompt 模板有效，换模型不用改代码
2. **内容层面不可信**——0.5b 数学推理空泛，与任务 B 的结论一致（必须有 7b+）
3. **发现"垃圾进垃圾出"的实证样本**：选择题第 1 题（id=12）的题干是 OCR 碎片
   （`Ox100001` + 残缺公式），AI 仍然一本正经地"解析"出了错误原因和变式题——
   **AI 不会说"题目看不清"，它会编**。这说明 OCR 质量是整条链路的上限：
   切分召回率 75% 意味着有 25% 的题根本没进库，而进了库的残缺题会诱导 AI 产生幻觉解析。
   对策：给 AI 的 prompt 增加"题目文本若残缺请明确指出"的约束。

**给组长的汇报要点**：0.5b 可验证接口协议与输出格式（9/9 格式达标，平均 6.9 秒），
但数学解析质量必须上 7b 及以上；且 OCR 准确率直接决定 AI 解析质量上限。

---

## 条目 18：A/B 实验——给 Prompt 加"残缺识别"约束，效果有限（2026-09-15 19:12~19:20）

**背景**：条目 17 发现题干残缺时 AI 会编造解析（垃圾进、幻觉出）。
假设：在 Prompt 里明确要求"看不清就说看不清"，能让模型停止编造。

**操作（严格的 A/B 设计）**：
1. 先把 v1 的 9 条解析结果导出快照 `data/ai_compare_v1.json`（否则会被覆盖）
2. **`explain_wrong` 原模板一字不改**，新增 `explain_wrong_v2`（保留四段结构，
   只在前面加 [重要前提]：文本来自 OCR 可能残缺，若残缺必须直接说明，严禁猜测）
3. 用 `--v2 --clear --all` 重跑同一批 9 道题，逐题比对

**结果**：

| 题 id | v1 字数 | v2 字数 | v1 是否承认残缺 | v2 是否承认残缺 |
|---|---|---|---|---|
| 12（最残缺，题干 `Ox100001`） | 495 | 414 | ❌ 编造 | ❌ **仍编造** |
| 13 | 904 | 504 | ❌ | ❌ |
| 14 | 449 | 839 | ❌ | ❌ |
| 15 | 774 | 229 | ⚠️ 提到"不完整" | ✅ "残缺，无法解析" |
| 16 | 411 | 633 | ❌ | ✅ "残缺，无法解析" |
| 17 | 769 | 817 | ❌ | ❌ |
| 18 | 405 | 343 | ❌ | ❌ |
| 19 | 552 | 357 | ❌ | ❌ |
| 20 | 1306 | 1067 | ❌ | ❌ |

**承认残缺的题数：v1 1/9 → v2 2/9**（另有一题 v2 首次调用超时，被容错逻辑捕获并记入 ai_logs 的 ok=0）

**分析（本次最重要的结论是负面的）**：

1. **约束有效，但远不够**：9 题里只多"救"回 1 题
2. **最该救的那题没救回来**：id=12 是残缺最严重的（题干是 `Ox100001` 加碎片公式），
   v2 依然一本正经地编出了"正确解题思路"。原因推测：碎片里含有 `f(x₀+∆x,y₀+∆y)` 等
   完整数学片段，模型能顺着脑补成一道偏导数定义题；且 0.5b 未必真正理解那段约束
3. **结论：不能把系统可靠性寄托在"模型自觉"上**。Prompt 约束是软约束，
   模型能力越弱越靠不住。可靠做法是在**入库前做工程化的质量门禁**：
   用启发式规则（非常规字符占比、置信度、题号缺失、长度异常）给每道题打
   `ocr_quality` 分，低分题**不送 AI**，直接标记为"需人工确认"。
   这样即使模型想编，也拿不到低质输入——**用代码保证下限，而不是用 Prompt 求模型自觉**

**后续行动**：在 schema 增加 `ocr_quality` 字段 + 实现打分函数，重跑验证拒答率

---

## 条目 19：OCR 质量门禁上线——用代码保证下限（2026-09-15 19:20~19:30）

**背景**：条目 18 证明 Prompt 约束救不回最残缺的题，结论是"不能靠模型自觉"。
本条实现条目 18 提出的对策：**入库前打分，低分题不送 AI**。

**实现**：新建 `src\backend\ocr_quality.py`，5 条可解释的评分规则（满分 100，逐条扣分）：

| 规则 | 扣分 | 依据 |
|---|---|---|
| 可疑字符（日文假名/韩文/带重音拉丁）占比 >5% / >2% | -30 / -15 | OCR 认错字最常吐这些 |
| 中文占比 < 10% | -20 | 题干通常有中文说明，纯符号说明切到的是公式碎片 |
| 行首无题号 | -15 | 题号被丢失或误识别（实测 "3." 被认成 "2."） |
| 疑似乱码串（字母数字混杂 ≥5 位）每个 | -10，上限 -30 | 如 `Ox100001`、`xbsy6` |
| 文本 < 30 字符 | -20 | 多半是被切碎的残片 |

设计原则：**每条规则都能说出为什么扣分**，调阈值时有据可依，也方便向老师解释系统为什么拒答。

**集成方式**：
1. `error_items` 增加 `ocr_quality` 字段（老库用 `ALTER TABLE` 自动迁移，已写进 schema.sql）
2. `ocr_to_db.py` 入库时即计算分数
3. `ocr_ai_batch.py` 送 AI 前先过门禁，低分题写入
   `[需人工确认] OCR 可信分 X 低于阈值`，`ai_model` 记为 `quality_gate`

**实测结果（阈值 70，9 道题）**：

| id | 质量分 | 结果 | 主要扣分 |
|---|---|---|---|
| 12（选择题1，题干 `Ox100001`） | **45** | 🚫 拦下 | 可疑字符 + 中文 0% + 2 个乱码串 |
| 13 | 100 | ✅ 送 AI | — |
| 14（选择题4，只剩选项块） | 65 | 🚫 拦下 | 中文 0% + 题号缺失 |
| 15 | 80 | ✅ 送 AI | 中文占比 4% |
| 16（填空2，公式残缺） | 65 | 🚫 拦下 | 可疑字符 + 中文 2.4% |
| 17 | 90 | ✅ 送 AI | 1 个疑似乱码串 |
| 18 / 19 / 20 | 100 | ✅ 送 AI | — |

**最终：送 AI 6 道，拦下 3 道。** 最关键的是 **id=12 被拦下**——
它正是条目 17/18 里 AI 编造得最起劲的那道题，现在根本拿不到。

**已知偏差（待优化）**：
"中文占比 < 10% 扣 20" 这条对**数学题天然不利**（数学题干中文本来就少）。
目前统一阈值，可能误拦部分完整数学题。后续可按学科设不同阈值，
或把"中文占比"换成更通用的"语义完整度"指标。

**意外收获**：本次重跑有 1 次调用超时，被容错逻辑捕获并记入 `ai_logs`（`ok=0`），
补跑成功 —— 说明"失败也要留证据"的设计是对的，失败样本同样是调优材料。

**方法论沉淀（可直接用于结题报告）**：
> 提高 AI 系统可靠性有两条路 —— ①让模型变得更聪明（换 7b、改 Prompt）
> ②让垃圾输入进不了模型（质量门禁）。
> 本项目实测：路线①在 9 题里只多救回 1 题；路线②直接把最坏样本挡在门外。
> **结论：先用工程手段守住下限，再谈模型能力。**

---

## 条目 20：质量门禁规则迭代——修掉对数学题的误判（2026-09-15 19:25~19:35）

**问题**：条目 19 留了个已知偏差——"中文占比 < 10% 扣 20 分"对数学题不公平
（数学题干中文本来就少），实测把 id=16（填空2，有题号有"设"字）误拦了。

**修法**：把"看中文多不多"换成"**看有没有题干关键词**"
（`设 / 求 / 已知 / 则 / 证明 / 计算 / 判断 / 下列 / 若 / 当 / 函数`）。
判据更本质：数学题中文再少，"设""求"这类词一定有；纯公式碎片则一个都没有。
**好处是不分学科通用**，不用为数学/英语分别设阈值。

**迭代前后对比**：

| id | 旧分（中文占比） | 新分（题干关键词） | 变化 |
|---|---|---|---|
| 12 | 45 🚫 | 45 🚫 | 一致（真残缺，两版都拦） |
| 14 | 65 🚫 | 65 🚫 | 一致（只剩选项块，无题干） |
| **16** | **65 🚫 误拦** | **85 ✅ 放行** | **误判已修正** |
| 15 | 80 ✅ | 100 ✅ | 更准确 |
| 其他 | 不变 | 不变 | — |

**最终结果：拦下 3 道 → 2 道，送 AI 6 道 → 7 道。**
被拦的 2 道（id=12、14）经人工核对确认**确实没有题干**，拦得对。

**方法论沉淀**：
> 规则类算法上线后要回头看"被它拦下的样本"——误拦（False Positive）比漏拦更隐蔽，
> 因为它不会报错，只会让系统悄悄少干活。
> 本次正是对着 3 个被拦样本逐个核对，才发现 id=16 是被冤枉的。
> **门禁类规则必须定期人工抽检被拦样本**。

---

## 条目 21：AI 兜底漏题——0.5b 输出不稳定，兜底失败但产出有价值的负结论（2026-09-15 19:35~19:50）

**目标**：把切分漏掉的 3 道题（选择2/填空3/计算2）找回来。

**实现**：`src\backend\ocr_recover.py` + 新增 AI 场景 `judge_fragment`
（判断片段是"上一题延续 / 独立新题 / 无法判断"）。
设计上加了两道保险：
1. **可救性预筛**：OCR 残留 < 20 字符直接判定"信息已丢失"，**不送 AI**
   （对着 `B.C` `8ふ` 这种碎片推理，只会得到幻觉——条目 18 的教训）
2. **默认演练模式**：不写库，人工确认后加 `--apply` 才入库

**结果**：

| 漏题 | OCR 残留 | 处理 |
|---|---|---|
| 选择题 2（行28-29） | 6 字符 `B.C` `8ふ` | 信息已丢失，需人工补录 |
| 填空题 3（行60） | 6 字符 `2×y334` | 信息已丢失，需人工补录 |
| 计算题 2（行69-84） | 61 字符（手写笔记+碎片） | 送 AI，**三次运行三种结论** |

**同一段文本、同一个 prompt，0.5b 三次输出**：

| 轮次 | AI 输出 | 判定 |
|---|---|---|
| 1 | "B 题干：3. 设f(x,y)=ln\|x+y\|-e…" | B 独立新题（但**抄了下一题的题干**，错的） |
| 2 | "A. 它是上一题的延续（作答过程…）B. 它是…"（**复述 prompt**） | 被我的关键词逻辑误判成 B |
| 3 | 输出不以 A/B/C 开头 | 无法判断 |

**踩坑（代码 bug）**：第 2 轮 AI 只是把 prompt 里的选项背了一遍，
我的判定逻辑却在里面搜到了"独立"两个字，误判为"新题"并**入库了**。
已回滚该条记录，并把判定改为**严格格式校验**：
```python
m = re.match(r"^\s*([ABC])\s*[\.、。:：]?\s", ans)
if not m or len(ans) > 400:   # 不以 A/B/C 开头，或输出过长（在复述 prompt）
    verdict = "unknown"
```

**结论（负结论，但很有价值）**：

1. **0.5b 不能承担"判断类"任务**——同样输入三次三种结果，无法作为系统决策依据
2. **判断类 Prompt 必须做输出格式校验**。只搜关键词会被模型的自说自话骗过；
   要求"必须以 A/B/C 开头且不过长"，等于给模型加了一道可机检的契约
3. **真正零成本有效的是"可救性预筛"**：不用模型就知道 2 道题没救，
   直接转人工——**知道"这事 AI 干不了"本身就是有价值的能力**

**产出**：`data/ocr_fast_test/pending_manual.json` —— 4 道需人工对照原图补录的题
（含题干残缺的选择题 1），预留 `stem` 字段待填。

**下一步**：这个兜底任务留给 7b 再试；当前系统靠 pending_manual.json 保证"漏题可见、不静默丢失"。

---

## 条目 22：单词线接入 + 跨线学情统计（2026-09-15 19:45~19:55）

**操作**：
1. `src\backend\word_import.py` —— 单词练习记录导入器，**支持真实数据**：
   能解析 QwertyLearner 导出的 Dexie JSON（`data.tables[].rows`，可选 gzip），
   映射到 `word_records` / `chapter_records`；库里没真数据时用 `--mock N` 生成演示数据
2. `src\backend\study_stats.py` —— 跨线学情报告（单词线 + 错题线 + 跨线汇总）

**结果（演示数据 200 条）**：
```
【单词线】练习 200 次 / 涉及 200 词 / 一次打对率 50.0% / 平均 1.7 秒每词
【错题线】数学 9 道 / 未掌握 9 道 / AI 已解析 7 道 / 门禁拦下 2 道
【跨线汇总】单词打对率 50.0%　数学错题 9 道　英语错题 0 道
```
200 条练习记录 **100% 关联到词库词条**（`word_id` 非空），说明词库和练习记录的映射正确。

**为什么值得单独记一条**：
这个统计脚本存在的意义，是**实证"单词和错题放同一个库"的价值**。
如果分两个库，"这个学生英语单词正确率多少 + 错题集中在哪个学科" 这类问题
必须分别查两个库再在 Python 里拼；现在只是几行 SQL，且后续加"学情诊断"模块时
可以直接把这份统计喂给 AI（`diagnose` 场景已经在 ai_interface 里预留了）。

**待办**：真实练习数据需从 QwertyLearner 前端导出（设置页有导出按钮），
导出后 `python src/backend/word_import.py --file 用户数据.json.gz --gz` 即可导入。

---

## 条目 23：人工补录 + AI 薄弱点诊断接入（2026-09-16 16:50~17:20）

**一、人工补录（4 道题处理完 2 道）**

`ocr_manual_import.py`：读 `pending_manual.json`，**只导入 stem 非空的条目**
（空白题干入库 = 制造脏数据，直接跳过）。人工录入的题 `ocr_quality=100`、
`ai_model='manual'`，便于和 OCR/AI 来源区分；按章节+题号去重，可重复运行。

| 题 | 处理 | 说明 |
|---|---|---|
| 选择题 1 | ✅ 已补录 | 基于原图起草题干（四个选项均为 lim 分式，判定为偏导数定义题），**标注待人工核对** |
| 填空题 3 | ✅ 已补录 | 原图疑似"设 f(x,y)=x²y⁵"，**指数看不真切，标注待核对** |
| 选择题 2 | ⬜ 未填 | OCR 仅剩 `B.C`，无法推断 |
| 计算题 2 | ⬜ 未填 | 该区域为手写笔记，无法推断 |

**原则声明**：起草的 2 道题干都标了 `_draft: true` 并写明依据，**没有把推测当事实**。
这正是条目 18 批评 AI"对着碎片编造"的同一条标准，自己也必须遵守。
错题总数 9 → 11 道。

**二、AI 薄弱点诊断接入**

1. 重构 `study_stats.py`：抽出 `collect_stats(cur, uid)` 返回结构化字典，
   统计脚本和诊断脚本共用同一份口径（避免两个脚本各查一遍、口径漂移）
2. 新建 `ai_diagnose.py`：统计 → `diagnose_weakness()` → 写 `ai_logs` + 存 Markdown 报告

**实测（0.5b，跑了两次）**：

| 轮次 | 耗时 | 输出质量 |
|---|---|---|
| 1 | 11.2 秒 | 出现"写作和阅读理解"建议 —— **数据里根本没有英语阅读数据** |
| 2 | 5.1 秒 | 出现"语料库问题""语法和词汇" —— 同样是数据里不存在的类别 |

**结论**：0.5b 在**诊断场景同样会幻觉**，而且比错题解析更危险——
错题解析编的是一道题，诊断编的是"学习建议"，用户更容易当真。
两次输出还不一致（条目 21 的稳定性问题再次复现）。
诊断功能**必须等 7b 才能真正可用**，当前只能验证链路和格式。

**踩坑**：`Path.parent` 层数算错两次（`src/backend/x.py` 要退两级才到项目根），
两次都是 `FileNotFoundError`。与 `__file__` 下划线问题是同一类"路径/拼写"错误，
写完先跑一遍再说完成。

---

## 条目 24：后端 API 层上线——从"脚本集合"变成"可被前端调用的服务"（2026-09-16 17:00~17:20）

**背景**：之前所有能力都是一个个 .py 脚本，只有我自己能跑；前端（wrong-notebook /
QwertyLearner）要接进来，必须有 HTTP 接口。

**技术选型**：FastAPI + uvicorn
（已装：fastapi 0.141.1 / uvicorn 0.52.4，清华源 7 秒装完）。
选 FastAPI 而不是 Flask 的原因：**自带 /docs 交互式接口文档**，
前端同学不用看我的代码，打开浏览器就知道每个接口怎么调。

**实现** `src\backend\api.py`，9 个接口：

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/health` | 服务 + 数据库 + Ollama 状态 |
| GET | `/stats` | 跨线学情统计（复用 collect_stats） |
| GET | `/errors` | 错题列表（支持按学科/掌握度筛选） |
| GET | `/errors/{id}` | 单题详情（含知识点标签） |
| POST | `/errors` | 新增错题（自动打分、打标签、排复习） |
| PATCH | `/errors/{id}/mastery` | 更新掌握度 |
| GET | `/review/due` | 待复习列表 |
| GET | `/words/wrong-top` | 易错词排行 |
| POST | `/words/practice` | 上报单词练习记录 |

**三条硬约束（写在文件头注释里）**：
1. 所有数据访问走 `db.get_conn()`，接口层不自己连库
2. AI 调用只能经过 `ai_interface`，接口层不直连模型
3. 写操作复用 `ocr_quality.score_text()`，与 OCR 入库保持同一套打分口径

**验证** `api_smoke_test.py`：9 个接口逐个真打，**全部 HTTP 200**。
关键返回值抽查：
- `/errors/12` → `ocr_quality=45`、标签正确带出"偏导数与高阶偏导数"
- `POST /errors` → 新题自动打分 85、自动排了 3 天后复习
- `/words/wrong-top` → carrier 等易错词连释义一起返回

**意义**：这一步之后，wrong-notebook 和 QwertyLearner 的前端改造
不再需要读我的 Python 代码，照着 `/docs` 调接口即可——
**接口即契约**，前后端可以并行开发。

**注意**：冒烟测试会往库里留一条测试错题（source 标着"接口冒烟测试"），
不影响使用，需要干净数据可手动删。

---

## 条目 25：补漏记录——一个数据安全隐患 + 环境备忘（2026-09-15 19:00 / 2026-09-16 补记）

> 说明：以下三件事当时只做了 git 提交，没写进日志，现补记。
> 第一条是**差点造成数据丢失**的隐患，必须留档。

### 一、`seed.py` 的破坏性默认行为（严重隐患，已修复）

**现象**：`seed.py` 第一句是 `DELETE FROM users`。因为外键级联，
执行它会连带清空 `error_items`、`error_item_tags`、`review_schedules`、`subjects`、
`knowledge_tags` —— **当时库里刚入库的 9 道真实错题会全部消失，且不报任何错**。

**发现过程**：在做"完整体检"时逐一审查脚本，发现 seed.py 与 ocr_to_db.py
共用同一个 test 用户，但 seed.py 无条件清库。任何人（包括未来的我）随手跑一次都会中招。

**修法**：改为默认不清库，只有显式 `--reset` 才重建：
```python
if reset:
    cur.execute("DELETE FROM users")
elif 用户已存在:
    print("[跳过] test 用户已存在，未做任何改动"); return
```
并在文件头注释写清两个脚本的执行顺序（reset 后要重跑 `ocr_to_db.py` 恢复真题）。

**实测验证**：修复后直接跑 `python src/backend/db/seed.py` →
输出 `[跳过] test 用户已存在`，`error_items` 仍为 9 条。

**教训**：
> **带外键级联的删除语句，必须有显式开关。** 脚本的"默认行为"应该是安全的，
> 破坏性操作要用户明确表达意图。这条现在是我写脚本的硬规矩。

### 二、完整体检结果（2026-09-15 19:00）

| 检查项 | 结果 |
|---|---|
| 数据完整性 | 1 用户 / 2 学科 / 1 知识点 / 9 错题 / 9 标签 / 9 复习计划 / 1 词库 / 2607 词 ✅ |
| **幂等性** | `import_dict.py`、`ocr_to_db.py` **各重复跑一次，数据不增不减**（2607 / 9 条）✅ |
| git 未跟踪文件 | `ai_interface.py`、11 个 `ocr_*.py`、`部署日志.md` 等 **16 个文件 1556 行**已补提交 |

**幂等性的意义**：脚本可以随便跑而不产生脏数据，这是"好脚本"的标准。
后面每加一个新脚本，都应该验证这条。

### 三、环境备忘（踩过才知道）

1. **Ollama 默认不启动**，每次开机要手动 `D:\Ollama\ollama.exe serve`；
   否则 `check_ollama_alive()` 返回 False，AI 相关脚本会走降级分支
2. **所有脚本必须在 `D:\bigcreat_project` 下执行**，用
   `venv\Scripts\python.exe src\backend\xxx.py`；cd 到子目录会报
   `无法加载模块"venv"`，cd 到文件会报"该路径不存在"
3. `Path(__file__).resolve().parent` 层数：`src/backend/xxx.py` 要退**两**级到项目根，
   `src/backend/db/xxx.py` 要退**三**级——今天在这里翻车两次
4. 提交时刷屏的 `LF will be replaced by CRLF` 是换行符转换提示，**无害，可忽略**

### 四、已交付文档清单（供 4 号写结题材料取用）

| 文档 | 内容 |
|---|---|
| `docs/第二阶段错误日志.md` | 本文件，25 条，含全部实测数据与踩坑 |
| `docs/部署日志.md` | 第一阶段环境部署全流程 |
| `docs/后端接口对接文档.md` | 9 个接口说明 + 字段约定 + 前端改造建议 |
| `docs/对接确认表.md` | 10 项待队友确认事项 |
| `docs/二号成员进度与对接清单.md` | 岗位分工对照 + 进度 + 缺口 |
| `data/ocr_fast_test/golden_splits.json` | 切分基准（人工标注 12 题） |
| `docs/学情诊断报告.md` | AI 诊断输出样例（0.5b，仅作链路证明） |

---

## 条目 26：响应 3 号前端清单——五大科目字段标识落地（2026-09-16 17:45~17:55）

**触发**：3 号黄谌佳《前端冗余模块删减清单》（2026-08-27，xlsx 4 个工作表）
的「待确认问题」表里有 **4 项点名 2 号确认**，一直挂着"待确认"。

**我的答复**：

| # | 3 号的问题 | 我的答复 |
|---|---|---|
| 1 | 要不要用户登录/账号体系？ | **不做**。本地单机单人，电脑即账号；库里保留 users 表但只有默认本地用户，前端不建登录页/个人中心 |
| 4 | 五大科目后端字段标识？ | **见下方方案**（纯后端问题，由我定） |
| 6 | login/register 页保留吗？ | **删**，同第 1 项（其第 11 项已给出同样结论） |
| 8 | tags 标签管理保留吗？ | **保留**，后端已实现 `knowledge_tags` + `error_item_tags`，现有错题都已打标签 |

**五大科目字段约定**（code 稳定、给前端筛选；name 中文、给界面显示）：

| code | name |
|---|---|
| `POSTGRAD_MATH` | 考研数学 |
| `POSTGRAD_ENGLISH` | 考研英语 |
| `POSTGRAD_POLITICS` | 考研政治 |
| `CET4` | 英语四级 |
| `CET6` | 英语六级 |

**实现**：
1. `schema.sql` 的 `subjects` 表增加 `code TEXT` 字段（新建库自带）
2. 新建 `src\backend\db\migrate_subject_code.py` 做老库迁移（幂等）：
   `ALTER TABLE subjects ADD COLUMN code TEXT` + 已有"数学"改名"考研数学"、
   "英语"改名"英语四级"，再补齐另外三个科目
3. `api.py` 的 `/errors` 与 `/errors/{id}` 返回 `subject_code`，
   并新增 `?subject_code=` 筛选参数（推荐前端用 code 筛选而非中文名）
4. `seed.py` 的学科插入同步为新方案

**验证**：
```
当前科目：POSTGRAD_MATH 考研数学（错题 12 道）/ CET4 / POSTGRAD_ENGLISH / POSTGRAD_POLITICS / CET6
按 code 筛选 POSTGRAD_MATH → 12 道；CET6 → 0 道
9 个接口冒烟测试全部 HTTP 200
```
迁移脚本重复运行不重复插入（幂等性验证通过）。

**两点额外发现**：
1. 3 号清单的「保留页面清单」里写明 **"核心 SM-2 算法由 2 号后端移植"** ——
   比统筹文档的"Anki 数据同步脚本"要求更高，**工时需从 4h 上调到 6~8h**
2. 该清单里 2 号姓名写的是「陈萱燕」，与本人自称 Mireya 不一致，
   **需核实**（避免结题时成果归属混乱）

---

## 条目 27：错题自动分类上线——并识别出"模型背模板"陷阱（2026-09-16 18:00~18:15）

**背景**：统筹文档里"错题数据分类逻辑开发"是 2 号第二阶段任务，一直没做
（字段 `error_type` 在 schema 里预留了但全为 NULL）。3 号前端的错题分布饼图也在等这个字段。

**实现** `src\backend\error_classify.py`：从 AI 解析文本里规则抽取四类
（concept 概念不清 / calculation 计算失误 / misread 审题偏差 / method 方法缺失）。

**关键设计 —— 识别"模板列举"陷阱**：

扫描 7 道已解析错题时发现，0.5b 经常这样输出：
```
2. 错误原因归类：概念不清，计算失误，审题偏差，方法缺失。
```
**这不是分类，是把四类当模板背了一遍。** 如果按关键词计数，这道题会同时命中四类，
分类统计直接失真。所以加了规则：
```python
if 同一段里出现 ≥3 个精确类别词: 判定为 template，分类置 NULL
```

**置信度机制**（新增 `error_type_conf` 字段）：
| 值 | 含义 |
|---|---|
| `high` | 只命中一个类别，可信 |
| `low` | 命中多个取最多的，或靠宽松词频推断 |
| `template` | 模型在背模板，未真分类 |
| `none` | 完全没有可用信息 |

**实测结果（7 道有 AI 解析的题）**：

| 分类 | 置信度 | 题数 |
|---|---|---|
| 未分类 | template | **5** |
| 计算失误 | high | 1 |
| 计算失误 | low | 1 |

**结论**：
1. 分类链路通了，脚本幂等，**等 7b 到位后重跑一条命令即可**（`python error_classify.py --apply`）
2. 但当前 0.5b 下 **71% 的题分不出来**——不是规则不好，是模型输出本身没有分类信息。
   **再次印证：不要给低质量输出做花哨的下游处理，先解决源头**
3. 分类不出来的题保持 NULL，**绝不瞎填**（与质量门禁同一条原则）

**配套改动**：
- `study_stats.collect_stats` 增加 `by_error_type` 分布，直接供 3 号画饼图
- `/errors` 返回 `error_type` 与 `error_type_conf`
- `/stats` 按学科统计验证通过

**顺手修的问题**：
冒烟测试 POST 用的旧科目名"数学"在库里产生了残留学科（1 道错题挂在"数学"下，
与 POSTGRAD_MATH 并存）。已把接口默认科目改为"考研数学 / POSTGRAD_MATH"、
冒烟测试同步更新，并把残留数据并入考研数学后删除该科目。

---

## 条目 28：补齐趋势图与时长图的数据基础——新增两张表（2026-09-19 21:30~21:45）

**背景**：3 号《前端图表数据需求清单》要求 6 个 ECharts 图表，其中
**「分科刷题正确率趋势图」和「备考时长堆叠柱状图」在库里没有任何数据源**
——只有错题和单词练习，没有"今天刷了多少题、对了多少、学了多久"。
上一版 `/stats` 对这两项返回空数组并在 `_missing` 里写明原因，**没有造假数据**。

**本次补建**：

| 表 | 用途 | 关键字段 |
|---|---|---|
| `quiz_records` | 刷题记录 → 趋势图 + 正确率 | total_count / correct_count / duration_sec / practiced_at |
| `study_sessions` | 学习计时 → 时长图 | duration_sec / studied_at |

新增两个上报接口：
- `POST /practice/quiz`（科目、总数、正确数、耗时）
- `POST /practice/session`（科目、时长）
建议**一次练习结束汇总上报一次**，不做每题一请求。

**工程细节**：
- 迁移统一执行 `schema.sql`（全是 `CREATE TABLE IF NOT EXISTS`），
  **不在迁移脚本里重复写 SQL**，避免两份定义漂移
- `practice_seed.py` 造了 14 天演示数据（33 条刷题 + 33 条计时），
  脚本头部**明确标注这是演示数据**，真实数据由前端上报

**验证**：
```
summary: 总刷题 664 题、正确率 62.5%、总时长 27.66 小时、今日 3.82 小时
trend:   33 条（按天 × 科目）
duration: 6 天（近 7 天，按天 × 科目小时数）
11 个接口冒烟测试全部 HTTP 200
```

**至此 6 个图表的数据覆盖**：

| 图表 | 状态 |
|---|---|
| 1 趋势折线 | ✅ 有数据（演示数据，待换真实上报） |
| 2 错题分布柱状 | ✅ |
| 3 分类饼图 | 🟡 等 7b 重跑分类 |
| 4 雷达图 | 🟡 recite / writing 两维无对应功能，返回 null |
| 5 时长堆叠 | ✅ 有数据（演示数据） |
| 6 薄弱考点 TOP10 | ✅ |

**注意**：演示数据必须替换成真实上报，否则答辩时"学情数据"是编的，
这一点已同步给 3 号。

---

## 条目 29：Anki 模块第一部分——SM-2 算法移植 + 卡片自动生成（2026-09-19 21:40~22:00）

**背景**：3 号《前端冗余模块删减清单》保留清单注明"核心 SM-2 算法由 2 号后端移植"，
统筹文档也要求"错词、错题、考点数据双向流转"。

**实现（三部分）**：

1. **`anki_sm2.py`** —— SM-2 纯函数，不碰数据库，好测：
   - 维护 ef（难度因子，初值 2.5，下限 1.3）、interval（间隔天数）、repetitions（连续答对次数）
   - `q < 3` 答错 → 次数清零、间隔回 1 天；`q >= 3` → 1 天 → 6 天 → 之后 ×ef
   - **易错点**：间隔要用**更新后**的 ef 算（我第一次写的注释就标错了预期值，已改）
2. **卡片表 `anki_cards`** + 三个接口 `/anki/cards`、`/anki/due`、`/anki/review`
3. **`anki_sync.py`** —— 错题 + 易错词自动生成卡片（幂等去重）

**自测结果（SM-2 四组）**：
```
新卡 q=4 → ef 2.50  间隔 1  次数 1
二轮 q=4 → ef 2.50  间隔 6  次数 2
三轮 q=3 → ef 2.22  间隔 13 次数 3   ← 6 × 2.22 = 13.32
答错 q=1 → ef 1.96  间隔 1  次数 0   ← 打回重来
```

**卡片生成结果**：13 张错题卡 + 50 张易错词卡 = **63 张**，今日到期 63 张。
幂等验证：再跑一次 → 新增 0、跳过 63 ✅

**修复的 bug**：`/anki/review` 传 `quality=9` 时，内部 `ValueError`
未被捕获，接口直接 **500 且返回空内容**——前端拿不到任何提示。
已加参数校验，现在返回 **400 + 明确错误信息**。
> 教训：**入口参数必须校验**，不能依赖内部函数抛异常。
> 内部抛的异常到了 HTTP 层会变成"500 + 空响应"，是最难排查的一类问题。

**验证**：14 个接口冒烟测试全部通过（含"非法 quality 返回 400"这一条）。

---

## 条目 30：Anki 双向同步——选 CSV 方案（2026-09-19 22:00~22:10）

**背景**：Anki 模块第一部分（SM-2 + 卡片 + 接口）完成后，还需要跟 Anki 桌面端互通。

**先查环境再定方案**（不凭空设计）：
```
Anki2 数据目录   未找到
Anki 程序        未安装
upstream/anki    只有源码，不是用户数据
```

**因此排除两条常见路线**：
- ❌ 直连 `collection.anki2`：本机没装 Anki，且该方案要求 Anki 完全关闭、schema 升级会失效，风险高
- ❌ AnkiConnect 插件：需要 Anki 运行 + 装插件，本机不具备条件

**选定：CSV 互通** —— Anki 桌面端原生支持「文件 → 导入 → CSV」，
**零依赖、零插件、不需要安装任何库**。

**实现**（两个脚本，都幂等）：
| 脚本 | 方向 | 说明 |
|---|---|---|
| `anki_export.py` | 我们 → Anki | 导出 CSV（正面/背面/难度因子/间隔/次数/到期），支持 `--due-only` |
| `anki_import_csv.py` | Anki → 我们 | 解析 CSV 取前两列，按 front 去重；兼容 utf-8-sig / utf-8 / gbk 三种编码 |

**验证**：
```
导出 64 张 → data/anki_export.csv（utf-8-sig 带 BOM，Excel 打开不乱码）
反向导入同一文件 → 新增 0、跳过 64  ✅ 幂等
--due-only → 62 张今日到期
```

**踩坑（第 3 次）**：`anki_export.py` 的输出路径又写错了 parents 层数
（`src/backend/` 下的脚本要退**三**级到项目根，写成了两级 → 文件跑到 `src/data/`）。
已改为**从 `db.py` 的 `DB_PATH` 推导项目根**，不再自己数层数。

> 教训：**同一个 bug 犯三次，说明不是粗心而是方法有问题**。
> 正确做法不是"下次小心"，而是**消除犯错的可能**——
> 让每个新脚本复用已被验证过的路径常量，而不是各自手算 parents。

---

## 条目 31：按队长 AI 协议改造接口层（2026-09-21）

**背景**：队长《AI模块对接回复.docx》下发对接协议，与我侧 9/15 版 `ai_interface.py`
存在 **4 处硬性冲突**，不改就联调不通：

| # | 队长协议 | 我侧原实现 | 后果 |
|---|---|---|---|
| 1 | scene = `error_analysis` 等五名 | `explain_wrong` / `diagnose` / `school_advice` / `essay_polish` | 场景对不上 |
| 2 | 请求带 `subject`、`user_context` | 只有 `payload`、`context` | 缺学科上下文 |
| 3 | 返回 `{code, scene, data, raw, model, elapsed_ms}` | 返回纯文本 `content` | 无法判断成败 |
| 4 | error_type 四 code | `concept` / `calculation` / `misread` / `method` | 分类值不合法 |

**改造**（`ai_interface.py` 整体重写，向后兼容旧调用方）：
- `SCENE_ALIASES` 做旧名映射，`ok` / `content` / `elapsed_ms` 三个属性全部保留，
  所以 `ocr_ai_batch.py`、`ocr_recover.py`、`ai_diagnose.py` **一行不用改**
- `FORCE_JSON = True` → Ollama 请求带 `format=json`
- `extract_json()` 处理模型三种脏输出：```json 围栏、前后加话、纯散文
- `validate()` 按 `SCENE_SPECS` 逐场景校验；特别拦「error_reason 里出现 ≥3 个类别词」
  （模型在背模板，不是真分类）
- `MAX_RETRY = 1` 重试；耗尽后降级：`data=None`、`content=""`、raw 保留原文

**验证**（`ai_protocol_test.py`，桩函数不依赖真模型，8 组全过）：
```
[1] 合法 JSON → code=0，只调 1 次
[2] 首散次合法 → 重试生效（共 2 次），code=0
[3] 两次都散 → code=1，data=None，content 为空串，raw 保留原文
[7] 服务不可达 → code=1 且不抛异常（跑批不中断）
```

**实测发现（负面结果，如实记录）**：
按新规则重跑库里 7 道已解析错题——**只有 1 道能自动入库**（id=13，conf=0.6），
**6 道被判待人工复核**：5 道 conf=0（模型把四类当模板列举），1 道 conf=0.4（仅宽松词）。
结论：**0.5b 在自由文本模式下几乎产不出可信分类**，`format=json` 强制四选一后应能改善，
等 7b 到位重跑才有比较意义。

**顺带修掉的两个隐患**：
1. `study_stats.py` 里 `e.error_type='calculation'` 和 `type_label` 都硬编码旧值，
   迁移后统计会静默失真 → 改为新 code 且兼容旧值
2. `api_smoke_test.py` 每次跑都往库里留脏数据（新增错题 + 刷题 + 计时 + 复习卡片），
   污染 3 号前端的统计数字 → 加跑前快照 / 跑后自动清理。
   本次顺手删掉 **6 条历史遗留**的冒烟测试错题（`source='接口冒烟测试'`，id 24~29）

**踩坑**：项目路径不在 `C:/Users/HP/WorkBuddy/2026-09-15-16-35-36`，实际在
`D:/bigcreat_project`（一开始按旧路径找 `ai_interface.py` 找不到，浪费了几步）。
虚拟环境用 `D:/bigcreat_project/venv/Scripts/python.exe`。

---

## 条目 32：真机联调——format=json 通了，但分类不可复现（2026-09-21）

**背景**：改造完协议后第一次连真实模型（qwen2.5:0.5b，Ollama 0.33.2）跑。
此前所有结论都来自桩函数或自由文本模式，没验证过真实模型。

**结果（三条，两好一坏）**：

1. ✅ **format=json 有效**。之前自由文本模式 7 题 6 题判待复核，
   开 format=json 后 6/7 题一次通过格式校验。队长的约束是对的。

2. ❌ **分类不可复现**。5 题 × 3 次，**只有 1/5 题三次分类一致**。
   一道手写的干净题（定积分）三次跑出 misread_question /
   concept_misunderstanding / method_gap 三个不同结果。
   ——不能归因于 OCR 质量，是模型能力问题。

3. ❌ **error_confidence 不能当入库闸门**。模型会在随机分类上给满把握：
   id=16 第一次 calculation_error/0，第二次 method_gap/1.0。
   置信度本身也在 0.0~1.0 之间跳。自评信号有意义（干净题 1、残题 0），
   但拦不住"抽错了还自信"。

**顺带发现并修掉的三个问题**：

1. `error_type_label` 与 `error_type` 错配。实测模型给出
   `misread_question`→"函数定义错误"、`method_gap`→"计算失误"。
   协议没约束两者必须一致。**修**：新增 `normalize_error_analysis()`，
   以 code 为准按 `ERROR_TYPE_CODES` 覆盖 label，不信模型的。
   顺带把 confidence 收敛成 float、knowledge_points 强转成数组。

2. `AIResponse` 缺 `retries` 字段。联调脚本要统计重试次数时报
   `AttributeError: 'AIResponse' object has no attribute 'retries'`。
   **修**：补字段，成功路径记实际重试次数，降级路径记 MAX_RETRY。

3. 偶发 124 秒超时。40 次调用出现 2 次（约 5%），卡在
   REQUEST_TIMEOUT=120 边界判降级。0.5b 正常 5 秒出结果，
   说明是 Ollama 侧卡顿不是 prompt 太长。**暂不调超时**，记录观察。

**A 层裸测的预兆信号**（当时没重视，事后看是关键）：
```
{"solution": "calculation_error", "error_confidence": 1,
 "error_reason": "lim(0→x) sin(0)/x = 0", "method_gap": false}
```
JSON 合法，但 error_type 字段根本没出现、分类值被塞进 solution、
还多出 `method_gap: false`。**"JSON 合法"不等于"内容对"，
校验器只查字段存在性是不够的。**

**对条目 31 结论的修正**：
条目 31 写的是"6 道待复核是因为 0.5b 在背模板"——那是自由文本模式下的结论。
真机开 format=json 后原因变了：**格式问题解决了，剩下的是不可复现性**。
结论（不能自动入库）没变，但催 7b 的理由必须换成"分类不可复现"，
而不是"模型背模板"，否则经不起队长追问。

**新增文件**：
- `src/backend/ai_live_test.py` 真机联调（A 层裸测 + B 层全流程）
- `src/backend/ai_repeat_test.py` 可复现性专项（N 题 × M 次）
- `docs/真机联调报告.md` 完整报告

**踩坑**：`docs/回复队长_AI协议确认.md` 上一次提交时 commit message 里写了
"docs: …回复队长…"，但文件实际没落盘（提交清单里没有它）。
**提交信息不能当作文件已存在的证据**，这次已补写。
（同次检查还发现 `10天冲刺计划.md`、`50天学习和开发计划.md` 在工作区被删，
HEAD 里还在，需要的话 `git checkout -- <文件>` 可恢复。）
